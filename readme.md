# avm

"amplify version manager"/nvm for Amplify CLI

```console
curl -fsSL https://github.com/josefaidt/avm/raw/main/install.sh | sh
```

```console
➜  avm
Usage: avm [options] [command]

Manage @aws-amplify/cli versions

Options:
  -h, --help                   display help for command

Commands:
  bin                          get Amplify CLI binary path
  install [options] <version>  install a specific version of @aws-amplify/cli
  use [options] <version>      use a specific version of @aws-amplify/cli
  list                         list installed versions
  help [command]               display help for command
```
